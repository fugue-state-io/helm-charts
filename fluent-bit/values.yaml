config:
  service: |
    [SERVICE]
        flush        1
        daemon       off
        log_level    info
        parsers_file parsers.conf

  inputs: |
    [INPUT]
        Name tail
        Path /var/log/containers/*.log
        Tag kube.*
        Parser docker
        DB /var/log/flb_kube.db
        Mem_Buf_Limit 10MB
        Skip_Long_Lines On

  outputs: |
    [OUTPUT]
        Name s3
        Match *
        bucket your-s3-bucket-name
        region us-east-1
        total_file_size 5M
        upload_timeout 10m
        store_dir /buffers
        s3_key_format /logs/$TAG/%Y/%m/%d/%H/%M/%S
        use_put_object On
        compression gzip
        tls On

  filters: ""   # <--- THIS removes default filters
  parsers: ""   # <--- Optional: if you're not using built-in ones

extraVolumeMounts:
  - name: buffers
    mountPath: /buffers

extraVolumes:
  - name: buffers
    emptyDir: {}
env:
  - name: AWS_ACCESS_KEY_ID
    valueFrom:
      secretKeyRef:
        name: fluent-bit-s3-credentials
        key: AWS_ACCESS_KEY_ID
  - name: AWS_SECRET_ACCESS_KEY
    valueFrom:
      secretKeyRef:
        name: fluent-bit-s3-credentials
        key: AWS_SECRET_ACCESS_KEY
