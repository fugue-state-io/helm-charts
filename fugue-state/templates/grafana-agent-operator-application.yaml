apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-agent-operator
  namespace: argocd
spec:
  project: fugue-state
  destination:
    namespace: loki
    server: https://kubernetes.default.svc
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: grafana-agent-operator
    targetRevision: 0.44.2  # check latest with `helm search repo grafana-agent-operator`
    helm:
      values: |
        installCRDs: true
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
