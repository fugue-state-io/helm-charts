{{- if .Values.fluentbit.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "log-persistence.serviceAccountName" . }}
  labels:
    {{- include "log-persistence.labels" . | nindent 4 }}
  {{- with .Values.fluentbit.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
