apiVersion: v1
kind: Service
metadata:
  name: {{ include "fugue-state-app.fullname" . }}
  labels:
    {{- include "fugue-state-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fugue-state-app.selectorLabels" . | nindent 4 }}
